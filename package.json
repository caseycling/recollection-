{
  "scripts": {
    "start": "concurrently \"cd server && npm start\" && \"cd client && npm start\"",
    "build": "cd client && npm run build"
  }
  // "devDependencies": {
  //   "cypress": "^9.5.4"
  // }
}
