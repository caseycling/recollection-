{
  "scripts": {
    "start": "concurrently \"cd server && npm start\" && \"cd client && npm start\"",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "cypress": "^9.5.4"
  },
  "dependencies": {
    "concurrently": "^7.1.0",
    "react-dom": "^18.0.0",
    "react-scripts": "^5.0.1"
  }
}
