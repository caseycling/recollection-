{
  "scripts": {
    "client": "cd client && npm start",
    "server": "cd server && npm start",
    "start": "start-server-and-test client server",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "cypress": "^9.5.4"
  },
  "dependencies": {
    "concurrently": "^7.1.0",
    "react-dom": "^18.0.0",
    "react-redux": "^8.0.0",
    "react-scripts": "^2.1.3",
    "redux": "^4.1.2",
    "redux-thunk": "^2.4.1",
    "start-server-and-test": "^1.14.0"
  }
}
